<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Android 12+ SplashScreen API configuration.
        windowSplashScreenBackground     — solid black matching the image bg
        windowSplashScreenAnimatedIcon   — the book+lock PNG (288dp, centred)
        windowSplashScreenIconBackgroundColor — same black so no ring shows
        postSplashScreenTheme            — hands off to the main Compose theme
    -->
    <style name="Theme.LibraVault.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">#000000</item>
        <item name="postSplashScreenTheme">@style/Theme.Libravault</item>
    </style>
</resources>
