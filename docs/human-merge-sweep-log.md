# Human-merge sweep log

Every state-changing action performed unattended by
[`.github/workflows/human-merge-sweep.yml`](../.github/workflows/human-merge-sweep.yml)
(`.claude/agents/human-merge-sweep-agent.md`) — a merge, a stale/superseded
close, a needs-info retry, or a circuit-breaker escalation — appended
right after the action by the agent itself. Posting a comment does NOT
get a row here (it's already
visible on the issue/PR itself); this log is specifically for the actions
that change pipeline state without a human directly triggering them. A
durable, human-readable audit trail, distinct from
[`auto-merge-log.md`](auto-merge-log.md), which records the fully
`risk:low` auto-merge path inside `principal-review.yml`. Every entry here
represents something Claude did a second time, on a schedule, standing in
for a human step at the end of `docs/agent-team-pipeline.md`'s state
machine — never a product/scope decision. Part 3's needs-info handling
adds a row for a retry (a bounded, at-most-once, reversible label change)
and for a circuit-breaker escalation (`status:needs-info` → `status:
escalated` after 3+ lifetime occurrences — irreversible by this
automation, only a human undoes it); a plain clarifying comment left for
a human to answer does not get a row, since it changes nothing and is
already visible in place.

| When (UTC) | Item | Title | Action | Reason |
|---|---|---|---|---|
| 2026-08-20T07:07:58Z | [#332](https://github.com/LibraVault/reader/pull/332) | fix(ios): EPUB reader shows real page count, not chapter count | closed (superseded) | Issue #331 already closed by merged [#333](https://github.com/LibraVault/reader/pull/333), an identical `TextPaginator`-based fix. Backfilled: the run that closed this (first end-to-end `workflow_dispatch` test) predated this log-entry instruction existing. |
| 2026-08-20T07:08:33Z | [#295](https://github.com/LibraVault/reader/pull/295) | fix(ios-reader): guarantee an exit from Markdown reader when toolbar hides | closed (superseded) | Issue #293 already closed by merged [#296](https://github.com/LibraVault/reader/pull/296), identical `.simultaneousGesture` → `.highPriorityGesture` fix. Backfilled: the run that closed this (first end-to-end `workflow_dispatch` test) predated this log-entry instruction existing. |
| 2026-08-24T12:59:01Z | [#507](https://github.com/LibraVault/reader/pull/507) | feat(ios): let user pick a specific system TTS voice | closed (superseded) | Issue #506 already closed by merged [#510](https://github.com/LibraVault/reader/pull/510), same title/feature. This PR had gone `CONFLICTING`/`DIRTY` against `dev`. |
| 2026-08-24T12:59:01Z | [#511](https://github.com/LibraVault/reader/pull/511) | fix(player): stop lockscreen media tile from showing a duplicate Play button | retry (capacity skip) | `status:needs-review` re-applied — the most recent comment was a Phase 4 concurrency-cap skip (`3/3 active runs`) with no later progress/label change since, not a real question. |
| 2026-08-24T16:43:06Z | [#511](https://github.com/LibraVault/reader/pull/511) | fix(player): stop lockscreen media tile from showing a duplicate Play button | merged | Reviewed diff and regression test for real: `.remove(Player.COMMAND_PLAY_PAUSE)` matches the diagnosed root cause, new Robolectric test confirmed red pre-fix / green post-fix, no sensitive paths touched, `risk:low` correct, QA + principal review both passed. CI green, `mergeStateStatus: CLEAN`. |
| 2026-08-24T16:43:06Z | [#530](https://github.com/LibraVault/reader/issues/530) | Code review cleanup batch: 8 low-severity findings (L1-L8) | retry (capacity skip) | `status:ready-for-dev` re-applied — the most recent comment was a Phase 4 concurrency-cap skip (`3/3 active runs`) with no later progress/label change since, not a real question. |
| 2026-08-24T16:43:06Z | [#5](https://github.com/LibraVault/reader/issues/5) | [enhancement] Autoscroll for ePub and PDF | retry (capacity skip) | `status:ready-for-dev` re-applied — the most recent comment was a Phase 4 concurrency-cap skip (`3/3 active runs`) with no later progress/label change since, not a real question. |
| 2026-08-24T20:38:09Z | [#539](https://github.com/LibraVault/reader/pull/539) | fix(vault): close VMK zeroing + cross-screen lock races (#525, #526) | escalated | Hit `status:needs-info` 3 times over its lifetime (2026-08-24T19:29Z, 19:52Z, 20:09Z) — circuit breaker fired. Swapped `status:needs-info` for `status:escalated`; no further automated action. |
| 2026-08-24T20:38:09Z | [#543](https://github.com/LibraVault/reader/pull/543) | feat(vault): unify EPUB/PDF/Markdown reading via ContentSource, delete VaultReaderScreen (#505 Phase 1) | retry (needs-info) | `status:needs-qa` re-applied — `status:needs-info` was applied because qa-agent's run completed without producing a verdict (agent-level failure, no real QA finding), judged transient. |
| 2026-08-25T08:53:53Z | [#580](https://github.com/LibraVault/reader/pull/580) | fix(vaultstore): catch Keystore-key-deleted race during PIN unlock | merged | Widened catch to `KeyStoreException`, matches the existing `KeystoreKeyLostException` pattern one line above (no throttle bump, routes to `KeystoreKeyLost`). Regression test constructs a fake wrap whose `unwrap()` throws mid-unlock; confirmed it fails without the fix. `core/vaultstore/**` sensitive-path, `risk:high` correctly applied. CI green, `mergeStateStatus: CLEAN`. |
| 2026-08-25T08:53:53Z | [#571](https://github.com/LibraVault/reader/pull/571) | fix(vault): make the "Screen Security" toggle take effect immediately | merged | Diff itself (`core/storage`, `feature/vault`) is correct and well-tested (verified by principal review, re-checked independently). PR body and principal review both surfaced a separate, already-filed CONFIRMED finding (issue #581, `security` label — `feature:reader`'s `ReaderScreen` never applies `FLAG_SECURE` at all) that forced `status:needs-human-merge`; that finding doesn't touch this PR's own diff and is already tracked independently, so merging this PR on its own merits. CI green, `mergeStateStatus: CLEAN`. |
| 2026-08-25T08:53:53Z | [#568](https://github.com/LibraVault/reader/pull/568) | fix(vaultstore): unlock throttle off-by-one gave 4 free attempts, not 3 | merged | One-line boundary fix (`<=` → `<`), QA independently proved the new test fails on pre-fix source and passes post-fix, principal review walked the backoff arithmetic by hand. `core/vaultstore/**` sensitive-path, `risk:high` correctly applied. CI green, `mergeStateStatus: CLEAN`. |
| 2026-08-25T08:53:53Z | [#595](https://github.com/LibraVault/reader/issues/595) | [enhancement] Chapter TTS Phase 1: Markdown — adapt to shared chapter model | retry (needs-info) | `status:ready-for-dev` re-applied — this issue's own triage comment said the only blocker was #593/PR #594 (shared `ReaderChapter` model) landing, not a scope question. PR #594 merged 2026-08-25T08:29:40Z, after the triage comment; `ReaderChapter` now exists in `dev`. |
| 2026-08-25T08:53:53Z | [#596](https://github.com/LibraVault/reader/issues/596) | [enhancement] Chapter TTS Phase 2: EPUB — TOC-based chapters + TOC sidebar | retry (needs-info) | Same as #595: sole stated blocker (#593/PR #594) has landed; `status:ready-for-dev` re-applied so dev-agent re-triages for real. |
| 2026-08-25T08:53:53Z | [#597](https://github.com/LibraVault/reader/issues/597) | [enhancement] Chapter TTS Phase 3: PDF — text extraction + page-based chapters, enable Read Aloud | retry (needs-info) | Same as #595: sole stated blocker (#593/PR #594) has landed; `status:ready-for-dev` re-applied so dev-agent re-triages for real. |
| 2026-08-25T12:57:46Z | [#576](https://github.com/LibraVault/reader/pull/576) | fix(vaultcrypto): drop retained vmk field from VaultFileReader | merged | Read the full `VaultFileReader.kt` source to confirm `vmk` was referenced nowhere outside `init` — the field-drop is behavior-preserving. New `VaultFileReaderKeyMaterialTest` genuinely exercises both claims (no retained field via reflection, `close()` zeroes `fileContentKey`) and was verified red-then-green per the PR body. `core/vaultcrypto/**` sensitive-path, `risk:high` correctly applied. CI green, `mergeStateStatus: CLEAN`. |
| 2026-08-25T16:45:16Z | [#610](https://github.com/LibraVault/reader/pull/610) | test(reader): extract PdfReaderScreen decision logic + coverage (#606) | merged | Principal review's CONFIRMED finding (#613: 0-page PDF still crashed in default `PAGINATED` scroll mode after this PR's own `coercePageIndex` fix) was already pushed as a follow-up fix by the time this sweep looked — verified the guard (`pageCount <= 0` checked before `PdfPaginatedView`/`PdfScrollingView` dispatch) and its two new Robolectric tests (paginated + scrolling, real 0-page PDF bytes) actually reproduce and cover the crash. `risk:low` label is now stale (diff grew to 415 lines post-fix, over the 400-line auto threshold, so a re-run of `classify_pr_risk.py` returns `risk:high`), noted here since it doesn't change the merge call — no sensitive path touched either way. Rest of the extraction (tap-zone/aspect-ratio/error-message/page-indicator logic) is a behavior-preserving 1:1 port, confirmed by diff read. CI green, `mergeStateStatus: CLEAN`. |
