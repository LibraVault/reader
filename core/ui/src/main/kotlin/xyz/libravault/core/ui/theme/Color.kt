package xyz.libravault.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand — warm leather & aged gold ─────────────────────────────────────────
val LeatherBrown      = Color(0xFF8B5E3C)   // primary — mid leather
val LeatherDark       = Color(0xFF4A2E14)   // primaryContainer — dark hide
val LeatherLight      = Color(0xFFF5E6D3)   // onPrimaryContainer — pale parchment

// ── Accent — antique gold ─────────────────────────────────────────────────────
val VaultGold         = Color(0xFFE8A830)   // secondary — glowing monogram gold

// ── Neutrals — warm, not cool grey ───────────────────────────────────────────
val WarmNeutral900    = Color(0xFF1A0E04)   // near-black leather
val WarmNeutral700    = Color(0xFF3D2208)   // dark brown
val WarmNeutral500    = Color(0xFF7A5C3E)   // mid brown
val WarmNeutral200    = Color(0xFFE8D5BC)   // warm off-white
val WarmNeutral100    = Color(0xFFF5EDE0)   // parchment
val WarmNeutral50     = Color(0xFFFBF7F2)   // near-white warm

// ── Sepia (reading mode) ──────────────────────────────────────────────────────
val SepiaBackground   = Color(0xFFF5EDD6)
val SepiaText         = Color(0xFF3B2F1E)

// ── Surface tokens (dark mode) ────────────────────────────────────────────────
val DarkBackground    = Color(0xFF1A0E04)   // very dark leather
val DarkSurface       = Color(0xFF2A1405)   // dark cover brown
val DarkSurfaceVar    = Color(0xFF522A0D)   // lighter cover (WCAG AA compliant: 5.2:1 vs #FFFFFF)
