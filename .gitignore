# Gradle
.gradle/
build/
**/build/
local.properties
*.hprof
.caches/

# SQLite databases (JVM Room test artefacts, never commit)
*.db
*.db.lck
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Android Studio / IntelliJ
.idea/
*.iml
*.iws
*.ipr
captures/
.externalNativeBuild/
.cxx/
*.DS_Store

# Keystore — NEVER commit signing keys
*.jks
*.keystore
keystore.properties

# Firebase service account key — NEVER commit
firebase-key.json

# Pro license private seed (NEVER commit; see tools/README.md)
libravault_pro_private.hex
tools/*.private
tools/*.key

# Generated
app/src/main/assets/index.android.bundle

# Kotlin / Java
*.class
out/

# OS
Thumbs.db
.DS_Store
.kotlin/

# Server .env (never commit secrets)
server/.env

# Node
node_modules/
server/node_modules/

# Test/release APKs at repo root
*.apk
feedback-1-changes.html

# Claude Code
.claude/

# Kilo CLI (agent plans, scratch files)
.kilo/
clear

# Xcode (per-machine user state, never commit)
xcuserdata/
*.xcuserstate
DerivedData/
*.xcscmblueprint

# Pocket TTS iOS build-time dependencies - fetched by
# third-party/sherpa-onnx/setup-ios.sh, not committed (~230MB combined,
# see SHERPA_ONNX_SETUP.md for why).
/third-party/sherpa-onnx/ios/
/third-party/sherpa-onnx/build-ios/
/ios/LibraVaultApp/LibraVault/LibraVault/PocketTTSModel/
